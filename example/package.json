{
  "name": "example",
  "private": true,
  "scripts": {
    "build": "assetline index.scss",
    "prepare": "npm run build",
    "lint": "stylelint --quiet --syntax scss **/*.scss",
    "test": "npm run lint"
  },
  "devDependencies": {
    "assetline": "file:../",
    "stylelint": "~10.0.1",
    "stylelint-config-rowupper": "~0.0.1"
  }
}
